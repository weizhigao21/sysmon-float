# 系统性能监控悬浮窗 - 开发文档

## 1. 项目概述

开发一款 Windows 桌面小工具，以**悬浮窗**形式常驻屏幕，实时展示 CPU、GPU、内存等核心硬件的使用率与温度（可选）。窗口置顶、无边框、可拖拽、低资源占用，适合作为日常性能看板。

## 2. 功能需求

### 2.1 核心功能（MVP）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| CPU 监控 | 整体使用率、各核心使用率 | 高 |
| 内存监控 | 使用率、已用/总量 | 高 |
| GPU 监控 | 使用率、显存占用、温度 | 高 |
| 悬浮窗展示 | 置顶、无边框、可拖拽、可调整大小 | 高 |
| 图表显示 | 实时折线图展示最近 60 秒数据 | 中 |
| 右键菜单 | 切换显示项、暂停/恢复、退出 | 中 |
| 开机自启 | 可选 | 低 |

### 2.2 非功能需求

- **低资源占用**：自身 CPU < 1%，内存 < 100MB。
- **低刷新延迟**：默认 1 秒刷新一次，可配置。
- **稳定性**：长时间运行不崩溃、不内存泄漏。
- **可扩展性**：后续方便添加网络、磁盘、温度等监控项。

## 3. 技术选型

### 3.1 推荐方案：Python + PyQt6

| 组件 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.10+ | 开发快、生态丰富、你已有经验 |
| GUI 框架 | PyQt6 | 支持无边框窗口、透明背景、置顶、自定义绘制 |
| 系统数据采集 | psutil | CPU、内存、磁盘、网络数据稳定可靠 |
| GPU 数据 | pynvml（NVIDIA）/ pyadl（AMD）/ wmi（Intel） | 先支持 NVIDIA，再逐步扩展 |
| 实时图表 | pyqtgraph 或自定义 QPainter | pyqtgraph 轻量、性能好 |
| 打包工具 | PyInstaller | 你已有使用经验 |

### 3.2 备选方案

- **C# + WPF**：与 Windows 集成更好，GPU 监控有 NVAPI 等原生支持，但开发周期较长。
- **Electron + node-systeminformation**：跨平台好，但资源占用高，不适合悬浮窗常驻。

> 建议：用 Python + PyQt6 做 MVP，快速验证需求；如后期对性能/原生体验有更高要求，再考虑迁移到 C#。

## 4. 架构设计

```
┌─────────────────────────────────────────┐
│              UI 层（PyQt6）              │
│  ┌─────────────┐  ┌──────────────────┐  │
│  │ 悬浮窗主界面 │  │ 图表 / 数字展示  │  │
│  └─────────────┘  └──────────────────┘  │
└─────────────────┬───────────────────────┘
                  │ 信号/槽（数据更新）
┌─────────────────▼───────────────────────┐
│           数据采集层                    │
│  ┌────────┐ ┌────────┐ ┌────────────┐ │
│  │ CPU    │ │ Memory │ │ GPU        │ │
│  │ psutil │ │ psutil │ │ pynvml/... │ │
│  └────────┘ └────────┘ └────────────┘ │
└─────────────────────────────────────────┘
```

### 4.1 模块划分

```
sysmon_float/
├── main.py                 # 程序入口
├── app.py                  # QApplication / 全局配置
├── ui/
│   ├── float_window.py     # 悬浮窗主窗口
│   ├── metric_label.py     # 单指标展示组件
│   └── mini_chart.py       # 迷你折线图组件
├── core/
│   ├── collector.py        # 数据采集调度器
│   ├── cpu_monitor.py      # CPU 监控
│   ├── memory_monitor.py   # 内存监控
│   └── gpu_monitor.py      # GPU 监控
├── config.py               # 配置管理
└── utils.py                # 工具函数
```

## 5. 关键实现方案

### 5.1 悬浮窗特性

使用 PyQt6 的窗口标志实现：

- `Qt.WindowType.FramelessWindowHint`：无边框
- `Qt.WindowType.WindowStaysOnTopHint`：置顶
- `Qt.WindowType.Tool`：不在任务栏显示
- `Qt.WindowType.WindowTransparentForInput`（可选）：鼠标穿透
- `Qt.WidgetAttribute.WA_TranslucentBackground`：透明背景

### 5.2 数据采集

- 使用 `QTimer` 每秒触发一次采集。
- CPU、内存通过 `psutil` 获取。
- GPU 优先使用 `pynvml`：
  - 安装：`pip install nvidia-ml-py`
  - 获取利用率、显存、温度。
- 未检测到 GPU 或驱动不支持时，优雅降级（隐藏 GPU 面板）。

### 5.3 数据展示

- 每个指标显示：名称 + 当前值 + 迷你折线图。
- 折线图使用 `pyqtgraph` 或 `QPainter` 绘制最近 N 个采样点。
- 根据使用率改变颜色：低绿 / 中黄 / 高红。

### 5.4 拖拽与交互

- 重写 `mousePressEvent` / `mouseMoveEvent` 实现窗口拖拽。
- 右键菜单提供：设置、隐藏/显示某指标、退出。
- 双击可切换紧凑模式与详细模式。

## 6. UI/UX 与风格设计建议

### 6.1 推荐风格方向

| 风格 | 特点 | 适用场景 |
|------|------|----------|
| **深色科技风（推荐）** | 半透明深色背景、霓虹色指标、细线图表 | 最耐看，长期驻留不刺眼 |
| **极简数字仪表风** | 大字号数值、无多余装饰、单色或双色 | 信息密度高，一眼可读 |
| **系统原生风** | 浅灰/白色背景、圆角卡片、阴影 | 与 Windows 11 视觉统一 |

> 首推**深色科技风**：悬浮窗常年在屏幕前方，深色背景不易分散注意力，且与游戏/全屏场景更协调。

### 6.2 推荐配色方案（深色科技风）

| 元素 | 颜色 | 色值 |
|------|------|------|
| 背景 | 深灰半透明 | `rgba(30, 30, 30, 0.88)` |
| 边框 | 暗色细线 | `#333333` |
| 文字主色 | 浅灰白 | `#e0e0e0` |
| 文字次色 | 中灰 | `#888888` |
| CPU 指标 | 青蓝 | `#4fc3f7` |
| 内存指标 | 紫罗兰 | `#ba68c8` |
| GPU 指标 | 橙黄 | `#ffb74d` |
| 高温/高占用警告 | 红 | `#ef5350` |
| 中等占用提示 | 黄 | `#ffca28` |

### 6.3 布局建议

- **默认尺寸**：240 × 120 px（紧凑模式），可拖拽缩放。
- **信息层级**：
  - 第一行：指标名称 + 当前百分比（大号等宽字体）
  - 第二行：迷你折线图（最近 60 秒）
  - 第三行（详细模式）：温度、频率、显存等辅助信息
- **三种模式切换**：
  - **极简模式**：仅显示百分比数字
  - **标准模式**：数字 + 迷你图
  - **详细模式**：数字 + 图表 + 辅助数据

### 6.4 字体与排版

- **主字体**：等宽字体，如 `Consolas`、`JetBrains Mono`、`Microsoft YaHei Mono`
- **字号**：
  - 百分比数值：16~20px
  - 指标名称：11~12px
  - 辅助信息：10px
- **对齐**：右对齐数值，左对齐标签，折线图统一宽度。

### 6.5 交互与动效

- **拖拽移动**：按住窗口任意位置拖动，松开后记住位置。
- **悬停反馈**：鼠标悬停时边框轻微高亮（`#555555`），提示可交互。
- **数据更新动效**：折线图和数字变化建议平滑过渡，避免闪烁。
- **右键菜单**：提供切换模式、选择显示项、透明度调节、退出。
- **透明度调节**：建议范围 60%~100%，默认 88%。

### 6.6 圆角与阴影

- **圆角**：8~12px，现代感强。
- **阴影**：轻微外阴影 `rgba(0, 0, 0, 0.3)`，增加层次感。
- **边框**：1px 半透明边框，避免窗口融入深色壁纸。

### 6.7 其他细节

- **边缘留白**：8~12px，内容不贴边。
- **图标**：可选小图标（如芯片、内存条、显卡），增强识别度。
- **颜色自适应**：当某项指标超过阈值（如 80%）时，该项颜色自动变黄；超过 95% 变红。

## 7. 依赖清单

```text
PyQt6>=6.4.0
psutil>=5.9.0
pyqtgraph>=0.13.0
nvidia-ml-py>=12.0.0
pywin32>=306          # Windows 特定功能（开机自启）
```

## 8. 开发计划

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| 第一阶段 | 搭建项目结构、实现无边框置顶悬浮窗 | 1 天 |
| 第二阶段 | 集成 psutil，完成 CPU/内存监控与展示 | 1 天 |
| 第三阶段 | 集成 pynvml，完成 NVIDIA GPU 监控 | 1~2 天 |
| 第四阶段 | 添加图表、右键菜单、配置持久化 | 2 天 |
| 第五阶段 | PyInstaller 打包、测试、优化 | 2 天 |

## 9. 打包与部署

- 使用 PyInstaller 打包为单文件可执行程序：
  ```bash
  pyinstaller --onefile --windowed --name SysMonFloat main.py
  ```
- 注意包含 PyQt6 平台插件与样式资源。
- 建议打包后在干净 Windows 环境测试，确认无依赖缺失。

## 10. 风险与建议

1. **GPU 兼容性**：不同显卡厂商 API 不同。建议先支持 NVIDIA，再逐步适配 AMD/Intel，未识别时隐藏 GPU 项。
2. **权限问题**：某些温度数据可能需要管理员权限，采集失败时给出友好提示。
3. **性能消耗**：避免过于频繁采集，默认 1 秒一次；折线图数据点控制在 60~120 个。
4. **多显示器**：窗口位置保存需考虑显示器 DPI 与分辨率变化。
5. **长期运行**：注意定时器、QPainter、数据队列的内存管理，防止泄漏。
6. **防误触**：提供“鼠标穿透”开关，避免悬浮窗干扰其他操作。

## 11. 下一步行动

建议先实现一个最小可用版本：

1. 创建项目目录结构。
2. 用 PyQt6 实现一个可拖拽的置顶无边框窗口。
3. 用 `psutil` 显示 CPU 和内存使用率。
4. 跑通后，再叠加 GPU 监控和图表。

如果你同意这个方案，我可以直接帮你从零开始搭建项目骨架并写出第一版代码。
