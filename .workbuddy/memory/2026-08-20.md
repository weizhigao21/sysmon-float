# 2026-08-20

## 使用图标（3.ico）
- 项目 `系统性能监控-win-悬浮窗`（PyQt6 悬浮窗，CPU/内存/GPU/网络监控）。
- 根目录 `3.ico`（55KB）之前未被使用，本次接入：
  - `main.py`：新增 `_resource_path()`（兼容 PyInstaller `_MEIPASS` 与开发路径），`QApplication` 创建后 `app.setWindowIcon(QIcon("3.ico"))`。
  - `系统性能监控.spec`：`datas` 增加 `('3.ico', '.')`（运行时资源），EXE 段增加 `icon='3.ico'`（exe 文件图标）。
- 验证：py_compile 通过；实际启动日志确认「窗口图标已设置」；已用 `PYTHONPATH= python -m PyInstaller 系统性能监控.spec --noconfirm --clean` 重新打包（WorkBuddy 注入 sitecustomize 会阻断打包，需置空 PYTHONPATH，与本机其他 Qt 项目约定一致）。
- 运行日志位于 `~/.sysmon_float/app.log`，窗口位置记忆 `~/.sysmon_float/window_position.json`。

## 健康检查修复（v1.0.0）
- P0：collector 网络日志箭头方向反了；GPU 查询失败 history 断裂（补 0）。
- P1：gpu_monitor 加 close()+nvmlShutdown；collector stop→`@pyqtSlot shutdown()`，main.py 退出用 `QMetaObject.invokeMethod(BlockingQueuedConnection)` 线程内安全停止并释放 LHM/NVML；cpu 温度 5s 缓存；WMI AdapterRAM >4GB 溢出注释；GPU 不可用菜单禁用；日志改 INFO + RotatingFileHandler(1MB×3)。
- P2：metric_widget 布局滞回（>10 竖 / <6 横）；_parse_rgba 缓存为 self._bg_color；mini_chart 单点画圆点；network 基准带宽配置化 `NETWORK_REFERENCE_MBPS=400`；删 WINDOW_X/Y 与 collector 回调死代码；清理 run.log/test_redirect.log；新建 .gitignore（含 .dbg/）。
- 文档：README 补图标/结构/配置/CHANGELOG 链接；新建 CHANGELOG.md（v1.0.0）。
- 验证：py_compile + AST 通过；开发实例启动正常（INFO 级、图标加载 OK）；独立脚本验证 shutdown 路径（BlockingQueuedConnection 无死锁）；临时测试脚本已删。
- 注意：用户 14:44 运行过打包 exe（_MEIPASS 图标加载成功），旧 exe 与新实例混写同一 app.log，排查日志时注意区分。

## git 初始化
- 项目原非 git 仓库，已 `git init`，项目级身份 weizhigao21 / weizhigao21@users.noreply.github.com。
- 首次 commit `c7ec5b6`（v1.0.0），64 文件，工作树干净。
- 远程仓库待用户创建（推荐名 `sysmon-float`），拿到 URL 后 `git remote add origin <url> && git push -u origin main`（wincred + 代理 127.0.0.1:7897 双轨配置已生效于全局）。

## 推送完成
- 远程仓库 https://github.com/weizhigao21/sysmon-float 已创建，origin 已添加，分支 master→main。
- 推送成功：`c7ec5b6`（v1.0.0）+ `79f71de`（docs 工作日志），本地 main 与 origin/main 同步。
- 注意：全局 credential.helper 指向 `C:/Users/14937/.workbuddy/vendor/PortableGit/.../git-credential-wincred.exe` 路径不存在（store 时报 No such file or directory），但 push 凭据来自其他来源仍成功；如后续 push 失败需检查该 helper 配置。

## 打包.bat（从音声下载项目迁移改造）
- 用户将音声下载项目的 `打包.bat` 放入本项目，要求适配。已改造为系统性能监控专用：
  - spec：`音声浏览下载.spec` → `系统性能监控.spec`，命令加 `--clean`。
  - 产物：onedir → **单文件** `dist\系统性能监控.exe`（备份用 move 改名 `old_*.exe`，清理用 `del /q dist\old_*.exe`）。
  - 依赖检查：`import PyQt6, psutil, pynvml, wmi, clr`；pip 安装 `PyQt6 psutil nvidia-ml-py WMI pythonnet`。
  - 删除 onedir 专属的精简段（opengl32sw/translations，原脚本还有重复粘贴 bug）。
  - 新增 `set "PYTHONPATH="` 规避 WorkBuddy 注入 sitecustomize 阻断打包。
  - **编码必须 GBK**（chcp 936），用 python 转码写入，UTF-8 直写会乱码。
- 实跑验证：`python -c "subprocess.run(['cmd','/c','打包.bat'], stdin=DEVNULL, encoding='gbk')"` 跑通（Git Bash 直接 `cmd //c 打包.bat` 会因中文路径解析失败进入交互模式）。打包 50s 成功，新 exe 15:08 生成（46.1MB），旧备份自动清理。
- 打包前需先关闭运行中的旧 exe 实例（会锁定 dist 文件导致 move/写入失败）。
- 已 commit `b1c428e` 并 push。

## v1.0.1：网络卡片上下行跳动修复
- 根因：网络速度文本长度波动（`↓ 0 B/s` 6字 ↔ `↓ 123.4 MB/s` 11字）触发 MetricWidget 自动横/竖布局切换（>10 竖、<6 横），名称与数值位置每次刷新上下跳。
- 修复：`MetricWidget` 新增 `auto_layout: bool = True` 参数；网络卡片传 `auto_layout=False` 固定垂直布局（名称在上、数值在下），`update_value` 中 `_auto_layout=False` 时跳过切换逻辑。
- 上下行分行：下行 `↓ {recv}` 主值（大字号）+ 辅助行仅显示 `↑ {sent}`（不再混合在一行）。
- 版本：新增 `ui/version.py`（VERSION="1.0.1"），README/CHANGELOG 同步 v1.0.1（README 补打包.bat、46MB 实际大小）。
- 验证：py_compile OK；实跑 12s 稳定无异常（注意：早前一次 3s 自退是外部关闭，非代码问题）；已 commit `1af785b` push；`打包.bat` 重打包 49s 成功，新 exe 15:16 生成（46.1MB），旧备份清理。

## v1.0.2：网络上下行并排显示
- 用户新想法：上下行并排一行显示（利用 vertical 布局网络卡片右侧空白），比 v1.0.1 上下分行更紧凑。
- 实现：网络 `value_text` 改为富文本 HTML 单行 —— 下行 `<span 18px #81c784>` + `&nbsp;&nbsp;&nbsp;` + 上行 `<span 14px #b8b8b8>`；`detail_text=None`。QLabel AutoText 自动渲染富文本（文本以 `<span` 开头）。
- 配色调整（用户反馈上行 #888 太暗、18/11 字号差太大）：上行 → `#b8b8b8`（新增 `COLORS.net_sent`）、14px（新增 `FONT_SIZE_NET_SENT`，config 配置化）。
- 修复逻辑 bug：`_use_horizontal_top` 初始赋值与 `_build_top_layout(horizontal=True)` 矛盾（原设 `self._auto_layout`），改回 `True` 保持一致；`auto_layout=False` = 固定水平布局、跳过切换。
- 效果验证：QApplication.grab() 渲染截图确认并排效果；用户实跑确认满意。
- 版本 v1.0.2（ui/version.py + README + CHANGELOG 同步），commit `ca9b575` push，重打包成功（15:27，46.1MB）。





